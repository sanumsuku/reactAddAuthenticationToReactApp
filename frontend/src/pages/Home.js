import PageContent from '../components/PageContent';

function HomePage() {
  return (
    <PageContent title="Welcome!">
      <p>Browse Events project with -</p>
      <p>Authentication to react App from backend (thru node.js)</p>
      <p>React router dom</p>
      <p>route path define</p>
      <p>loader fns - data before the page show</p>
      <p>error page</p>
      <p>action fns - for add/edit/delete event</p>
      <p>details page - Defer, Await, Suspense, redirect hooks</p>
    </PageContent>
  );
}

export default HomePage;
